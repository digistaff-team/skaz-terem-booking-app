/**
 * Персональный месячный лимит часов бронирования на одного жителя —
 * независим от общего пула по помещениям (monthlyLimits.ts) и от
 * депозита часов (room_balances). Один общий лимит на все помещения.
 *
 * Информационный: бронирование при исчерпании НЕ блокируется, остаток
 * уходит в минус и подсвечивается в кабинете.
 */

export const PERSONAL_MONTHLY_LIMIT_MINUTES = 40 * 60; // 40 ч/мес на человека
